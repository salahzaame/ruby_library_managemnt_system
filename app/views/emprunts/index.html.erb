<div class="container">
  <h1>Tous les emprunts</h1>
  <ul class="emprunts">
    <% if logged_in? %>
      <% @emprunts.each do |emprunt| %>
        <li>
        <button class="btn btn-success rounded-pill px-3" type="button">
              <%= link_to link_to emprunt.id, emprunt ,class: "text-decoration-none text-white"%>
        </button>
          <% if @current_user.admin? %>
            <div class="my-3 p-3 bg-body rounded shadow-sm">
              <div class="d-flex text-muted pt-3">
                <svg class="bd-placeholder-img flex-shrink-0 me-2 rounded" width="32" height="32" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: 32x32" preserveAspectRatio="xMidYMid slice" focusable="false"><title>Placeholder</title><rect width="100%" height="100%" fill="#007bff"></rect><text x="50%" y="50%" fill="#007bff" dy=".3em">32x32</text></svg>
                <div class="pb-3 mb-0 small lh-sm border-bottom w-100">
                  <div class="d-flex justify-content-between">
                    <strong class="text-gray-dark"></strong>
                    <button class="btn btn-danger rounded-pill px-3 " type="button"> 
                    <%= link_to "delete", emprunt,class: "text-decoration-none text-white", method: :delete, data: { confirm: "confirmer" } %>
                    </button>
                  </div>
                </div>
              </div>
          <% end %>
        </li>  
      <% end %>
    <% end %>
  </ul>

  <%= will_paginate @emprunts, class: "pagination justify-content-center" %>

  <%= link_to "Nouveau emprunt", new_emprunt_path, class: "btn btn-primary" %>

  <%= form_with model: @emprunt, url: search_by_id_emprunts_path, class: "mt-3" do |form| %>
    <div class="input-group">
      <%= form.number_field :id, class: "form-control", placeholder: "Search user by ID" %>
      <button type="submit" class="btn btn-primary">Search</button>
    </div>
  <% end %>
</div>
